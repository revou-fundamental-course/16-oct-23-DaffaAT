var nav_item = document.getElementById("nav-item.msg");
var form = document.getElementById("message-us");

var scrollDown = nav_item.style.scrollBehavior;

nav_item.onclick = function()
{
    nav_item.style.scrollBehavior;
    scrollDown = form;
}